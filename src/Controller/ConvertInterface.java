package Controller;

import java.time.LocalDateTime;

/**
 * This is the interface class for the lambda expressions
 * */

public interface ConvertInterface {
    LocalDateTime Convert(LocalDateTime T);
}
